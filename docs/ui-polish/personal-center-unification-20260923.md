# 唯一个人中心与关联页面收口

## 范围

以用户明确选定的第二张个人中心为“我的”唯一根内容，Android/iOS共用实现。`VideoRuntimeAccountPage`只负责接入根页和环境专属入口，不再维护第二套个人中心和重复资料请求。保留可独立推入的`PersonalCenterPage`返回能力，但App正常入口不再多push一层。

| Before | After | Why |
| --- | --- | --- |
| 我的→头像/设置→第二套个人中心 | 我的直接显示完整个人中心；头像/资料直达编辑，设置直达账号安全 | 删除重复层级，保留真实返回路径与底栏 |
| 根页缺失图2装扮陈列；开发诊断占据首屏 | 装扮陈列、钱包/装扮/资料、房间、8工具统一；诊断只在调试开发环境底部 | 保持用户选定的视觉结构与正式环境边界 |
| 两张“最近进房”实际都打开收藏 | 一个收藏入口；仅有实际roomId时显示正在收听 | 不把同一收藏入口和装饰头像伪装为两条历史记录 |
| 小字体、32dp装扮CTA、硬接缝、固定四列 | 44dp CTA、圆角衔接、提高辅助文字可读性、自适应工具列数与底栏留白 | 小屏和1.3字号可达，不引入无意义动效 |
| 普通用户入口也写现金收益 | 我的只写钱包，钱包副标题按incomeEligible显示 | 不改变收益/提现能力门控、账本与资金规则 |

## 保留的行为

- ProfileDisplayReadFence、账号代次/请求代次保护；切回“我的”重新读资料，资料/关系/装扮返回刷新。
- 当前会话账号传递；退出仍由AppGate执行，根页不假造二级返回。
- 通知、隐私/黑名单、帮助、平台工作人员服务端权限入口。
- 资料加载失败仍可退出；平台权限入口不依赖社交资料成功。
- 不修改Backend、厂商、付款、收益与提现规则，不迁移数据。

## 验证

本机Flutter 3.44.7，checkout自身依赖，测试并发2。执行证据位于项目`artifacts/ui-audit-20260921/core8-refinement-20260923/personal-center-unification-20260923/`。

- `targeted-second.log`：12文件80PASS；`shell-and-linked-final.log`：8文件40PASS，两组有重复，不相加计唯一数。
- `behavior-final.log`：合并去重18文件104PASS/0FAIL/exit0，为最终本轮行为组；84组布局渲染包含通知中心6组。
- 新增7例实际MainShell根入口/资料/设置/通知/钱包角色/收藏/360×640字号1.3底栏触控；资料失败的平台入口/退出另增2例。
- 首轮旧路径断言和ABA夹具不匹配已修正；ABA使用可控新身份回包并保留迟到拒绝断言。一次错误测试文件名导致加载失败，非产品RED，已按实际文件重跑。
- `render-matrix.log`54布局渲染、`render-linked.log`24布局渲染；后续通知6组单列。均为真实Flutter Widget与合成数据，不冒充原生设备。
- Mac普通golden比较65PASS，只有根“我的”和US-001两张基准按新结构更新；全局比较阈值不变。首次发现共享圆角影响其他6页，已恢复共享默认值，不扩散视觉基准。
- analyze无问题；15个Dart文件format 0changed；diff-check通过。
- M4/M3.2/M3.3来源与验收规则合同116PASS；不等同于执行完整设备业务。两条设备登出流程让工具滚至视口中部，避免MainShell底栏覆盖。

新候选编译、Android实装和Linux/完整CI以追加回执为准。iOS DeviceHub本轮只读控制探测仍返回AX -10005，不能将Widget/编译结果称为iOS交互通过。未合并、未部署。

独立审查补充：M4/M3.2设备脚本使用了已删除的二层入口定位，已纳入本轮选择器迁移；不通过恢复假入口来满足旧脚本。M2.4旧脚本只同步本次钱包入口文案，不宣称整条历史集成套件本轮执行通过。

## 原生与CI增量

- 742788 Android/iOS Simulator均编译exit0；Android A/B覆盖安装，APK SHA-256为561236640ef9ce8be578d6f996fe81c8f551bd780a53ed07eb47faecfd50399b，两设备实际base.apk一致。iOS SE3覆盖安装，App.framework/App本机与设备均为94b16d8d94e55d805a603f500c58c05a9969525b944bae8528afc106c7891686。原账号/应用数据保留。
- Android B原生确认唯一根页、头像直达编辑、设置直达安全；钱包、装扮、收藏、关系、访客、通知、隐私和帮助均打开并返回同一根页。未购买/送礼/提现/修改资料/隐私，保留账号登录。首次批量脚本将通知入口文案误认为页面标题后停止；实际截图与XML明确为“系统与互动通知”，后续手动按已观察控件完成系统/互动页签及返回，记录为测试定位错误，不是产品失败。
- 原生发现内嵌GridView重复继承MainShell底部安全留白，工具卡片多出空白。补显式零内边距，外层列表仍保留底栏安全距离；仅根“我的”Mac基准有1.12%预期差异，逐图审查后更新。
- 742788 Linux候选269源码哈希核对一致，84张图片清单对比仅2张变化，下载对应2PNG并校验、逐图审查。普通比较实际63PASS/2FAIL正是两张旧个人中心；基准没有从Mac复制，没有降低阈值。后续根页padding变化仍需精确新候选Linux比较/原生安装回执。
- 全量CI还发现video_runtime_ui_test旧mock-only recent-room-880217定位。已实读旧源码确认该卡片来自硬编码_recentRooms且live环境不显示；将回归迁移至现行“收藏房间→仓库收藏→进入房间”，仍断言实际RoomPage及roomId=880217，不恢复伪历史、不删除进房断言。
- iOS原生控制本轮两次getApp均AX -10005；Jev dry-run/真实run/动作均0，无手动原生UI接管。只读simctl安装/启动/截图与Flutter自动化不能代替iOS点击验收。
- padding与现行收藏进房路径的最终4文件组83PASS/0FAIL/exit0（含65Mac普通golden），analyze无问题、format/diff-check通过；日志final-native-followup-green.log及analyze-native-followup.log。旧83项首轮唯一失败是已审根页视觉差异。

以上安装哈希绑定742788；追加padding修正后的最终包以外部最终验收回执为准。PR27保持Draft，未合并、未部署。
