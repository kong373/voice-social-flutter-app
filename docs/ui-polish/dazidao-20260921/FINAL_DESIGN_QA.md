# 搭子岛：最终视觉复查与证据边界

> 追加说明：发布前发现远端已推进至1df32a96294031318c939989594a960529d895e7（包含5cffe文档提交），已核对仅文档变化。本批不覆盖它们修正后的公共页面矩阵、FINAL_REVIEW_20260922或原进度文件。本文件及STATE_MATRIX_FINAL为补充证据索引；公共矩阵仍以最新远端版本为准。248是本批额外场景口径，不替代公共汇总的记录数。

## 当前结论

V01–V07在已记录场景中完成；V08真实麦位面板捕获已在前轮补齐。源码、声明的自动化状态和本机macOS视觉基准可审查。整体发布仍blocked：74张已审macOS PNG未同步到PR、Linux像素比较及设备验收未执行。

| Before | After | Why / 证据 |
|---|---|---|
| V01品牌缺字 | 固定来源字体子集覆盖当前源码和搭子岛 | 字体原门禁6通过3失败→9通过；来源/哈希保留，不声称生产设备缺字 |
| V02私聊气泡／回执对比不足 | 原共享品牌渐变，白色回执和时间 | 状态模型与已留存、待投递、失败、未知标签保留；7种delivery状态验证 |
| V03失效房间文字不清 | 深色Room文字色，加载时明确指示 | 实际前后图及读失败／加载场景验证；不改变有效链接跳转 |
| V04账号局部颜色分散 | 原有语义色；状态pill墨色文字；44px设置操作 | 协议／认证／权限正反状态和大字号表单验证 |
| V05搜索／公会渐变白字不清 | 保留渐变方向，合理选择前景与轻量底色 | 普通、主播、会长、待审与关闭状态，不引入新角色 |
| V06公告只读内容过淡 | 明确只读文字和label颜色 | enabled条件不改，不为可读性赋予编辑权限 |
| V07分隔线过重 | 显式轻／深DividerTheme | 消息恢复、支付、房间恢复及其他默认页面前后检查 |
| 更多面板双拖拽指示／麦位标签拥挤 | 移除该面板冗余系统handle；原按钮横向padding调整 | 保留已有麦位数量、入口和操作；并非全App所有Sheet均已重做 |

## 已实际查看

60页加4个真实shell入口的16张前后对照板；179种读取／状态代表画面的23张对照板；22种补充音频／恢复／PK／支付／订单场景的小屏放大字号画面；举报与反馈键盘焦点画面；Mac基准74对图片的19张对照板。
前后输入来自精确基线4e78573与最终实现，均为合成夹具原生Widget渲染，不是旧APK截图、静态概念稿、真实账号或厂商成功证据。
最后三处已记录的SnackBar字体／注册头像标题／发布类型标题变化在完整sealed123源码门禁中重验，未用前一版本测试冒充最终版本。

## 限制

实际viewports为375×667、390×844、402×874，字号1.0／1.3。六配置无overflow不等于每张图逐像素审查、所有控件都满足WCAG或全部复杂交互均被验证。
Mac基准更新前2通过63失败为视觉基准差异；逐张审查74个实际捕获后本机65通过。没有删除测试、放宽容差或批量忽略差异。Linux目录与9张历史删除页基准保持不变。
补充场景中的恢复controller、音频权限与PK结果是明确的测试投影，仅验证显示及禁用；真实RTC声音、治理生效、资金结算及跨账号端到端结果仍由设备／业务验收负责。
PNG同步拒绝记录、原失败日志及所有图像只保存在本机证据目录。远端PR不包含新的74张基准，不可写成CI或远端golden全绿。
