# 搭子岛全页面任务：2026-09-22接续点

任务未关闭，60页全量目标不缩减。最新成果继续追加至同一PR26，不合并，不晋升冻结RC。
本次UI生产差异只在commerce_earnings_pages.dart；新增两份状态/面板测试，扩展原渲染测试。前序基础/社交/房间源码原样保留。

## 不要重复执行

- 上一检查点90189e3已承接的22文件及默认页面验证不重新生成。
- 旧PR的Tooltip、提现和公会等定点修复不在本UI批重复实现或擅自叠加；是否已集成以当前候选为准。
- 不执行未完成的`refine_visual_pass.py`，也不通过更换工具重放被拒绝的同一操作。
- 最早直接把收款表单挂在浅色根主题的黑色截图是夹具背景问题，不能当成真实App可读性缺陷；正式比较已改为从提现页真实导航、与App一致的根主题。

## 本次已补齐

1. RM-005真实面板捕获：普通用户2–9号申请、待审/同意/拒绝显示，房主/房管1–9号选择。测试仅打开面板，不占麦、不审批、不触发RTC。
2. 不同房间角色的“更多”菜单和工具面板；房管/房主PK及治理入口仍在，普通听众没有被额外授权。
3. 普通用户、主播、会长的资产入口、历史/未知提现、加载/错误/空/无账户状态，以及支付宝/银行卡绑定表单。未知状态明确恢复原命令，不显示新申请已成功。
4. 收款表单从浅色资产流程进入时维持现有社交视觉体系，分隔说明与输入框；全部字段和本人实名规则不变。
5. 32个场景×6尺寸/字号=192项布局/状态；全部5个收款输入位置×6配置=30项模拟键盘焦点检查。实际矩阵与日志见相邻STATE_MATRIX_20260922文件和本机resume22-*日志。

## 外部依赖与仍需工作

| 剩余项 | 精确接续要求 |
|---|---|
| V01 测试字体缺搭字 | 复核既有合法字体覆盖及真实目标平台；不静默换字体、批量更新golden或把macOS结果当Linux通过 |
| V02–V07 已定位视觉问题 | 当前连接的修正写入曾被安全检查拦截，需Codex独立审查/执行；范围仍见design-qa.md的逐项位置，业务方法/角色/未知结果语义不得改 |
| 所有其他适用角色、状态和附属交互 | 状态增量仅覆盖本次32场景；依据原60页矩阵继续，不把未覆盖项随意写成N/A |
| Android/iOS构建与设备 | 需要明确独占窗口、实际设备和可用工具链；不接管其他任务窗口，Xcode许可不能代用户接受 |
| 真实键盘/返回/手势/辅助技术/profile | 模拟insets与Widget执行不能替代；本轮NOT_RUN |
| 商店及远端名称 | 客户端名称不代表商店、远端协议/分享文案已修改；不自动部署或改厂商配置 |

每次续接先回读checkpoint及当前远端HEAD、比对已提交文件哈希和未提交修改。只继续尚未完成步骤，不重放金融/治理命令。
