# 搭子岛：第一、二、三类收尾交付

本文件是2026-09-22最新增量。旧PROGRESS、RESUME及design-qa中的阶段失败保留，但不能覆盖本文件及对应实际日志。原60页范围、9个删除页、角色和资金规则不变。

项目基线：`4e78573f5fd00fffbe70c9ed274b7c31a7342fa8`。
前一已发布检查点：`c36d4f0192d4eb1cf8d78b191235ee9fe5fd72d9`。
分支：`codex/gpt-dazidao-ui-takeover-20260921`；交付PR26保持Draft、不合并。
最终提交SHA以PR回读和本机发布日志为准，不把父SHA写成新交付。
已核实源码与测试提交：`2ea29985c75a2247aee80d1fad7c40381d4e9da9`（13份表现层文件、6份测试文件）。后续文档提交不改变已验证代码。


## 第一类：已定位视觉问题

| Before | After | Why / 证据 |
|---|---|---|
| V01 测试字体缺“搭”字 | 复用项目既有固定来源字体构建器，补齐现有中文字符集，更新原有两个测试子集及来源哈希 | 修正前字体契约3失败，修正后9通过；未替换系统字体、未更新golden图片；生产系统字体另需设备核验 |
| V02 私聊白字叠浅渐变 | 复用现有brandGradient；本方回执/时间明确白色 | 消息正文、已留存、已读、实时未知/失败/送达含义不改 |
| V03 深色房间直达标题/原因过暗 | 明确RoomColors主/次文字；等待链接校验显示进度 | 原校验、跳转和入房权限不变 |
| V04 账号局部颜色脱离共享主题 | 主按钮与次文字复用现有语义色；状态标签保留色彩背景、采用可读正文色 | 不改变认证条件和可操作状态 |
| V05 搜索/公会浅渐变白字 | 搜索复用品牌渐变；公会浅色hero深字、深色详情hero白字 | 原角色、内容、模块顺序不变 |
| V06 只读公告难读 | 正文、标签和计数明确深色房间文字色 | enabled和canEdit原条件保留，未开放编辑 |
| V07 分隔线过重 | 在原Theme中补充social/room DividerTheme | 不另造组件或更改列表结构 |
| 更多面板双拖拽条、放大字号麦位标签挤压 | 保留原自绘拖拽条；选麦按钮减少水平内边距 | 没有删除工具入口或缩小既有触控高度 |

## 第二类：按实际页面补充状态与交互

全量入口仍来自 `lib/app/page_manifest.dart`。60页矩阵见 `PAGE_MATRIX.md`、`page-matrix.json/csv`；实际测试场景见 `verification-final-20260922.json`，不得将未列状态组合视为通过。
新测试通过真实Flutter页面与受控仓库读模型渲染，不是生成静态概念图；全部场景明确使用合成数据。新增状态采集拒绝消费/治理写入；既有契约测试仅操作隔离测试夹具。

覆盖读取加载/失败/空、身份角色、注销冷静期、实名认证、权限状态、七类实时回执、支付结果、装扮到期/停售、公会归属、音频可用性/禁用、重连、PK胜负/平局等。协议面板、注册/举报/发布校验、黑名单确认、榜单切换、真实麦位面板及资产入口分别保留独立证据。

### 状态检查中新修正

| Before | After | Why / 保留边界 |
|---|---|---|
| 成员请求尚未返回或失败，显示“0人在线”“全部0” | 显示加载/待确认；未确认时筛选标签不伪装零值 | 两项真实UI断言先失败；仅改显示，不改分页、权限、租约或请求 |
| 协议页正文末尾两链接在375/390宽、1.3字号溢出 | 原顺序保留，Row改为可换行Wrap | 六项实际布局失败先保留；不改变读完、勾选、协议版本和确认条件 |
| 永久拒绝权限的“打开设置”命中区过小 | 合并原状态与按钮为至少44px命中区 | 首次扩大后产生的布局回归已修正；实际六配置验证通过，不代替OS权限测试 |
| 小屏举报与反馈长文本框被键盘遮挡 | 键盘出现时缩短可见行数，关闭后恢复；文本与字数限制不变 | 真实模拟键盘失败已保存，新增12项草稿保留/恢复检查；原提交、媒体、身份隔离不变 |
| 浅色支付结果/说明、申诉原因、搜索标题、注册头像标题、发布类型继承外层深色文字 | 就近明确现有social文字色 | 只改已实际渲染确认的颜色来源，不改页面功能布局 |
| 排行前三名浅色数字难读、PK底部说明过暗 | 保留金银铜背景，数字改正文色；PK说明采用room次文字 | 金额、排行计算、比分和结算规则不变 |
| 自定义测试字体未传入SnackBar，瞬态错误提示呈缺字方框 | 两套主题的SnackBar沿用同一fontFamily参数 | 不更换字体、不改错误内容；生产未传自定义字体时仍使用原系统回退 |

## 第三类：最终源码、品牌、静态与视觉核对

正式结果只读取 `review123-*` 的退出记录和日志；已通过的历史运行与新运行分开，尺寸变体/重复运行不相加宣传为独立业务数量。Flutter3.44.7，测试并发2，串行执行，无数据库、设备或移动构建。
最终Dart源码哈希与测试前后快照核对，摘要在本机 `review123-source-stable.json` 和发布日志。标准仓库 `git diff --check` 使用本次独立目录自己的Git元数据，不再以no-index或普通空白扫描代替。
Android/iOS可见名称、页面清单、字体来源/覆盖、golden路径映射有契约测试。包名、applicationId、Bundle ID、签名、Entitlements、URL Scheme、IAP标识及内部订单/幂等标识未替换。未改Backend、CPS、数据库、厂商配置或消费/恢复逻辑。

## 验收边界与外部待办

代码完成、自动化通过、列示的视觉场景审查和设备通过分别记录。当前未做APK/IPA构建、真实设备/输入法/手势/辅助技术/性能测试，没有发生真实资金操作或厂商调用。
保留原macOS/Linux历史golden图片，未用批量更新掩盖差异。新增画面来自macOS原生Widget；历史golden逐像素复核/更新及Linux渲染属于明确未完成的独立证据，不能记作Linux通过。
被拒绝的逐场景keyboardFieldIndex接口扩展、Mock房间能力查询没有换通道重做；已有UI_KEYBOARD_FIELD接口的已知字段检查继续执行。未验证的条件字段及未列角色/状态组合不标PASS、不随意改成N/A。
设备接续由Codex在独占窗口执行：同最终SHA构建；检查登录/换号、房间治理和返回、公屏/私聊、礼物未知恢复、装扮到期、支付返回、提现状态和公会入口；不新发真实付款/出款。

## 本机证据根目录

`/Users/kongzheng/Documents/ny/artifacts/ui-audit-20260921/gpt-takeover-20260921/`
包括原始失败、成功日志、渲染PNG/JSON、前后图、源码哈希和发布回读；这些日志不是设备成功证据。中断恢复先读checkpoint和PR当前SHA，不重放已发命令。

## 最终执行结果索引

| 检查日志 | PASS | FAIL | 退出码 |
|---|---:|---:|---:|
| review123-final-format.log | — | — | 0 |
| review123-final-analyze.log | — | — | 0 |
| review123-final-supplement.log | 132 | 0 | 0 |
| review123-final-related.log | 482 | 0 | 0 |
| review123-final-interactions.log | 90 | 0 | 0 |
| review123-state-capture.log | 179 | 0 | 0 |
| review123-final-states.log | 1074 | 0 | 0 |
| review123-final-default.log | 64 | 0 | 0 |
| review123-final-layout.log | 384 | 0 | 0 |
| review123-final-sheets.log | 192 | 0 | 0 |
| review123-final-format-check.log | — | — | 0 |
| review123-financial-capture.log | 32 | 0 | 0 |
| close123-draft-retention.log | 12 | 0 | 0 |
| resume123-keyboard-0.log | 66 | 0 | 0 |
| close123-keyboard-1.log | 24 | 0 | 0 |
| resume123-keyboard-2.log | 12 | 0 | 0 |
| resume123-keyboard-3.log | 6 | 0 | 0 |

标准git diff --check与git diff --check HEAD均exit0，包括新文件。首次CSV的CRLF导致空白检查exit2，已将两份生成CSV统一LF；原失败记录保留，不记业务RED。
默认60页及4主入口的16张成对审查板已实际打开；22种补充状态和15种交互的小屏放大字号图已审查，最后三处字体/标题修正另读单图复核。497份采集索引不等于逐张逐像素或所有设备认证。
