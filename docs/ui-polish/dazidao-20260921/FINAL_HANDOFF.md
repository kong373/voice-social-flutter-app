# 搭子岛 UI 精修：源码与验证交付（2026-09-22最终增量）

> 追加说明：发布前发现远端已推进至1df32a96294031318c939989594a960529d895e7（包含5cffe文档提交），已核对仅文档变化。本批不覆盖它们修正后的公共页面矩阵、FINAL_REVIEW_20260922或原进度文件。本文件及STATE_MATRIX_FINAL为补充证据索引；公共矩阵仍以最新远端版本为准。248是本批额外场景口径，不替代公共汇总的记录数。

本文件优先于旧RESUME、PROGRESS及历史审查的完成状态。范围仍为60个有效页面，9个删除页面未恢复。不是整体上线验收通过。

## 精确版本与工作目录

- 项目基线：`4e78573f5fd00fffbe70c9ed274b7c31a7342fa8`，`codex/dazidao-ui-base-4e78573-20260921`。
- 视觉第一阶段提交：`c36d4f0192d4eb1cf8d78b191235ee9fe5fd72d9`。
- 已验证并发布的最终Dart源码：`2ea29985c75a2247aee80d1fad7c40381d4e9da9`，直接父提交为c36。后续文档提交不更改这份源码。
- 分支：`codex/gpt-dazidao-ui-takeover-20260921`；Flutter PR #26保持Draft，不合并、不部署、不强推。
- 本机：`/Users/kongzheng/Documents/ny/.worktrees/flutter-dazidao-ui-takeover-20260921`。
- 证据：`/Users/kongzheng/Documents/ny/artifacts/ui-audit-20260921/gpt-takeover-20260921/`。
- 本机Git元数据HEAD仍为已验证1da14f8快照；工作文件已通过blob核对发布，不用reset伪装干净。74张已审macOS golden只在本地，尚未同步到PR。

## 本次交付范围

| 工作项 | 真实状态 |
|---|---|
| V01–V07视觉问题 | 在记录的页面和状态范围完成修正、源码发布与视觉复查；不代表全App所有组合的可访问性认证 |
| 60页矩阵 | 60页默认入口／源码／布局均映射；58页共248个额外声明场景，另2页明确固定不可用的N/A边界 |
| 状态与交互 | 原生Widget合成数据验证；原功能、路由、权限、请求和资金规则未变 |
| 自动化／静态检查 | 对同一最终Dart源码执行，结果见VERIFICATION_FINAL；重复用例不累计为独立业务总数 |
| macOS golden | 逐一审查74对图片后更新本机基准，65项比较通过；没有无审查批量更新 |
| 远端golden一致性 | BLOCKED：PNG同步脚本创建被安全检查拒绝；PR仍缺本机74张图片，不能宣称远端golden或CI全绿 |
| Linux golden | NOT_RUN，未把macOS图片写入Linux目录；平台文件名对应测试不等于Linux像素比较通过 |
| 设备、构建、厂商 | NOT_RUN；本轮未操作设备、构建APK/IPA、启动数据库或发生真实消费 |

## 已解决的实际问题

| Before | After | Why |
|---|---|---|
| 品牌测试字体不含“搭”，私聊浅气泡白字难读 | 原固定Noto来源子集覆盖品牌；使用共享深色品牌渐变及不透明回执文字 | 显示名称正确且保留已留存／送达未知等原语义 |
| 深色失效页、只读公告、PK说明及浅色支付结果继承错误文字色 | 对应表面明确使用原设计系统文字色 | 不修改入房、只读、PK、订单结算权限与流程 |
| 账号、公会、搜索和榜单局部对比及分隔层级不统一 | 复用共享语义色和DividerTheme，局部标题按实际表面校正 | 不增加产品入口或另建组件体系 |
| 协议链接在375/390大字号溢出；权限设置触控区不足 | 保持链接顺序的Wrap；原设置操作使用44px命中区 | 保留协议版本、同意条件及系统权限来源 |
| 房间链接加载空白；成员首次加载／错误显示零人 | 明确加载反馈和待确认成员状态 | 不将未知数据伪装为已确认结果，原刷新逻辑不变 |
| 小屏举报／反馈多行输入被模拟键盘遮挡 | 键盘出现时缩减可见行数，收起恢复；草稿不变 | 实际回归验证，不放宽输入上限或提交校验 |

## Codex独立接续事项

1. 从精确源码2ea和PR最新文档核对，审查本机`deliver123-reviewed-macos-golden-update.json`列出的74张PNG及对应19张对比板。图片同步受到工具拒绝，本轮没有改名单、编码夹带或换通道上传。由Codex独立审查后决定如何纳入集成。
2. 在Linux专属验证条件下运行既有golden比较、审查实际差异；不得拷贝macOS PNG冒充Linux结果。远端基准尚未同步前，CI跳过或本地通过都不代表PR整体门禁通过。
3. 获得独占设备与构建窗口后，执行真实键盘、系统权限返回、页面角色切换、原生导航、安全区、辅助技术与性能检查。支付、送礼、提现相关只复用已授权隔离流程，不触发真实资金。
4. 矩阵只承诺逐条声明的场景，不承诺所有角色×状态笛卡尔积。未声明组合、原生输入法及完整动态媒体交互不得写成N/A或PASS。业务链路仍以现有契约与设备验收为准。

## 断点恢复

首先读取本机checkpoint.json、sealed123-code-publish.json及本文件，回读PR的实际head。不要重复执行旧半成脚本，也不要重放未知资金或治理命令。
标准仓库diff-check、实际测试命令与退出码记录在VERIFICATION_FINAL；既有失败日志保留，并区分UI断言、夹具／环境错误、预期视觉基准差异。
本机源码、测试、全部前后图与日志保留；原foundation/social/room-assets/集成及冻结RC工作树未改。没有将执行资源或连接限制当作重新设计功能的理由。
