## 2026-09-22 最新收尾增量（优先于下文历史快照）

源码、逐页矩阵与最终测试交付以 FINAL_REVIEW_20260922.md、DESIGN_SYSTEM_20260922.md 和 verification-final-20260922.json 为准。原V01–V07已在限定表现层修正并有新鲜渲染；举报/反馈键盘、协议换行、成员未知数量和局部主题字体修正均保留原业务语义。不要依据下文旧“未修复/无法运行”的段落重复开发。

最终同源码运行：482项相关回归；179个状态场景×6配置=1074；22个补充场景×6=132；15个交互×6=90；32个面板/资产场景×6=192；60页+4主入口默认渲染64及布局384。各组有重叠，不相加为独立业务数量。analyze与format检查exit0；最终源码哈希稳定。

60页JSON/CSV/Markdown及497份采集记录（312个不同场景标识，含默认页与主入口）已经整理。普通图不是设备验收，未列状态组合不声明通过。历史golden图未批量更新，Linux光栅、移动构建、真机输入法/手势/辅助技术仍NOT_RUN。

所有提交仍在PR26独立分支，未合并、未部署；最终SHA查看PR与本机发布回读日志。旧失败和拦截记录继续保留。接续只处理明确剩余事项，不重放被拒绝的脚本/查询，也不触发真实资金、厂商或共享工作树修改。

---

# 搭子岛 UI 精修：本轮视觉审查

final result: blocked

范围：60个有效页面的默认夹具，加4个实际MainShell入口。不是静态重新设计、设备验收或在线业务验收；9个删除页面没有恢复。

- source visual truth：本机证据目录下 `renders/exact-before/390x844-text1.0/`，生产源码为精确基线 `4e78573f5fd00fffbe70c9ed274b7c31a7342fa8`。
- implementation：`renders/matrix-first-pass/390x844-text1.0/`，生产源码检查点 `52b1dfd9ac38944d74a69370ad0f822dbcf23bec`。
- viewport：375×667、390×844、402×874，DPR=1；字号1.0和1.3。默认配对均390×844、同夹具、同时间、同macOS字体条件，无密度缩放差。
- data：明确使用现有SYNTHETIC_QA_FIXTURE；不是真实账号、资金、银行或厂商成功证据。
- font：现有M3GoldenCjk子集及MaterialIcons；没有更新任何golden、导入先前不明平台图片或宣称Linux通过。
- comparison input：`comparisons/account-01..03.png`、`ds-1..2.png`、`us-1..3.png`、`rm-1..3.png`、`ms-1..2.png`、`cm-1..3.png`、`sc-1.png`、`core-before-after.png`均为基线/当前并排，已实际打开审查。
- focused review：额外打开了四个核心页面单图、375×667/1.3私聊键盘图；仍须补齐各状态和更多局部控件，不以缩略拼图证明全部可访问性。

## Before / After / Why

| Before | After | Why / 当前结论 |
|---|---|---|
| 用户可见Voice Social名称 | AppBrand、登录、Android和iOS显示名为搭子岛 | 只改对外品牌；测试字体缺搭字，品牌视觉证据未闭合 |
| 多处共享文字/状态色对比不足 | 增强共享语义色；浅色社交、深色房间方向保留 | 9组平面色值检查达到4.5目标，不代表图片叠加/局部硬编码都通过 |
| 部分控件视觉尺寸也是小触控区 | 首页36px圆形视觉配独立44px命中区；房间30px pill配44px命中区 | 扩大可操作范围，不将所有视觉元素膨胀；回调/禁用行为保留 |
| 9处隐式过渡未读取减少动态偏好 | 复用AppMotion，偏好开启时这些视觉时长为0 | 正常时长与业务计时器不变，不延迟或重复业务命令 |
| 消息/关系列表分隔层级各异 | 承接已审的局部分隔/草稿和不可用标识 | 不改变消息成功、已保存、未送达含义；尚有局部Divider待统一 |
| 原生渲染准备停在fake-time之前 | 只修新测试的runAsync准备和有界图像等待 | 已能真实生成页面PNG，未放宽布局断言 |

## 必须继续处理的发现

| ID | 优先级 | 位置 / 证据 | 尚未完成的原因 |
|---|---|---|---|
| V01 | P1证据 | AC-003缺字框；`account-01.png` | 测试字体字符集确实缺搭字；未改字体或绕过字体门禁，不能把测试缺字认定为生产设备缺字 |
| V02 | P1 | MS-002发送气泡和回执对比不足；`ms-1.png` | 新视觉修正脚本追加被安全检查拦截，未执行，未换通道重试 |
| V03 | P1 | RM-003深底标题/原因过暗；`rm-1.png` | 同一未执行修正；需保留业务校验和跳转，仅修正文色来源 |
| V04 | P2 | 账号局部muted和按钮色；`account-02.png` | 局部色值仍独立于共享主题；未执行修正 |
| V05 | P2 | DS-002及SC-001/002/003浅渐变白字；`ds-1.png`、`sc-1.png` | 未执行修正，不改变公会角色或内容 |
| V06 | P2 | RM-008只读公告文字过淡；`rm-2.png` | 未执行修正；只读权限不能为了可读性变成可编辑 |
| V07 | P2 | MS-006、CM-003、RM-011局部分隔线过重 | 共享DividerTheme仍需明确；未执行修正 |
| V08 | 覆盖缺口 | RM-005仅渲染了父房间页 | 必须实际打开麦位面板；当前不能算该面板通过 |

## 五项视觉核对

- 文字：实际查看了60页默认渲染；品牌字体门禁、局部说明文字、气泡回执有上述开放项。全角色及系统字体条件未关闭。
- 间距/布局：384项尺寸/字号组合无捕获的布局异常；有滚动内容的全长/所有弹窗还需补证据。不能把无overflow等同每个按钮均可达。
- 颜色：共享语义对比已增强；硬编码局部渐变等仍有开放项；因此不声明全App满足4.5:1。
- 资产：保留原资产、头像及图标体系；未生成替代UI、未重做Logo、未修改旧golden。原图片的适用状态和实际用户媒体仍待设备核查。
- 文案/功能：并排默认图没有新增产品入口或角色。合成夹具中的支付/渠道说明不代表当前真实厂商能力；代码品牌更名不代表商店及远端协议已经改名。

## 交互与证据边界

新测试另运行36项模拟安全区/键盘/减少动态组合：六页×三尺寸×两字号，焦点是各页第一个真实EditableText，280px模拟键盘；全部通过。不是OS键盘截图，不覆盖所有字段、提交按钮或真实输入法。
现有业务相关定向批次最终349项通过；最初1项Android宿主自测受系统PATH的keytool影响，明确使用本机已安装OpenJDK21后该组60项全部通过；详见`verification-results.json`。不能将mock测试当作真实送达、RTC听到声音、支付到账或人工出款。
设备、APK/IPA构建、真实profile帧耗时、完整辅助技术及全部角色/状态验收均未完成。完整计划仍为WIP，不允许晋升上线候选。

## 修正历史

第一轮：承接基础/社交已有源码，复用主题与减少动态机制；组件测试通过。
第二轮：渲染辅助测试准备等待修正后，完成新鲜基线和当前默认对照及384项布局；发现V01–V08。
第三轮：增加独立模拟安全区/焦点输入验证，通过36项；上述新生产视觉修正没有执行，不能写为修复后复验通过。

后续只在原范围内处理上述可证明的视觉问题，保留所有业务保护；安全拦截项由Codex独立审查执行窗口后承接，不自动重放半成脚本。

## 2026-09-22 后续视觉审查

全局final result仍为blocked；V01–V07受限修正未重试。本节只更新已新增证据，不能用新局部通过覆盖旧开放项。

| Before | After | Why / 验证 |
|---|---|---|
| 原提现未知时警告“申请已禁用”，下方却是可操作“申请提现” | 显示原意图已锁定，按钮明确为“恢复原提现申请” | 当前页面真实渲染确认矛盾，3个角色UI回归从FAIL变PASS；只改说明和标签，不改原请求命令 |
| 从提现页进入收款绑定继承根深色主题，浮动收款类型标签紧贴说明 | 复用浅色资产表面，说明/字段间距16/12 | 实际点击已有导航后同390×844、同字体同内容对照；表单字段、规则、业务回调不变 |
| V08仅有RM-005父页面 | 实际打开可申请/等待/已同意/已拒绝以及房主/房管选麦面板 | 这项“实际面板捕获”缺口已补；不等于实际麦克风发布或整条审批设备验收 |

新增并排比较：`comparisons/resume22/withdrawal-unknown-before-after.png`、`payout-route-before-after.png`。两组源/目标均实际打开，390×844、DPR1、M3GoldenCjk，未缩放应用内容。

新增小屏放大字号审查：`comparisons/resume22/role-sheets-matrix-01..04.png`、`financial-matrix-01..04.png`逐张打开；32场景的375×667/1.3对应画面核对。收款表单以最终`renders/resume22-final-states/.../CM-012-payout-*.png`单图审查替代最早独立挂载时的黑色透明背景夹具，后者不作为真实App缺陷证据。其余最终画面与已审图逐一PNG哈希一致。

- 文字/颜色：未知状态不再前后矛盾；收款字段和本人账户说明可读；全App局部对比问题仍按V02–V07保持开放，未宣称全局4.5:1通过。
- 间距：收款说明与浮动标签不再紧贴，字段顺序未变；三尺寸、两字号均保留滚动能力，192次当前状态布局检查无异常。
- 资产：未新造图片、图标或字体；所有新数据均为明确的合成夹具。
- 功能：32场景涵盖普通/主播/会长资产入口、未知原申请，以及房主/房管/普通听众/平台角色的面板显示；没有点击金融确认、上麦占位或治理命令。平台角色夹具不代表额外platformStaff标志、关房访问等组合已验收。
- 键盘：收款两种类型的全部5个输入位置×6尺寸/字号组合共30次焦点检查通过。280px为模拟键盘占位，不是原生输入法图像。另实际查看375×667/1.3银行第三字段焦点图，输入区域未被模拟占位遮挡。

P3后续精修：更多面板存在双拖拽指示条；普通听众选麦数字标签在放大字号下换行为两行，但内容和触控仍可达。记录为后续细节，不把192项无overflow宣传成视觉零问题。

尚未完成：V01–V07修正后的全量对比、所有其他角色/状态及附属交互、真实设备/键盘/手势/辅助技术、APK/IPA构建和profile。原60页任务范围不变，不能晋升上线候选。
