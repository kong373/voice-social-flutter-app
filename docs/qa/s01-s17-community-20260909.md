# S01 / S17 Flutter 社区与名下多房交接

基线：`3c216fc7632494103e17ef9c1de820b9e09bed25`。
独立树：`/Users/kongzheng/Documents/ny/.worktrees/flutter-community-guild-policy-20260909`。
分支：`codex/flutter-community-guild-policy-20260909`。仅本地提交，不 push。

## 实际合同与实现

- Backend 只读核对快照 `301339cd67b7784d73149ba8165375ebaa74f548`：`FirstPartyRoomLifecycleController` / `FirstPartyRoomLifecycleService`。
- 名下列表使用既有 GET `/app-api/rooms/getRoomSelectByUserId`，`pageNum/pageSize`；保留所有分页与 OPEN/CLOSED。新选择 DTO 仅含 identity/state，不把列表当成编辑或入房授权。拒绝重复 ID、冲突 roomIdStr、未知状态和分页不一致。
- 首页既有创建入口现在先展示名下房间；明确选择“进入房间”或“管理房间”。前者仍交给 RoomPage 的 `enterRoom` 权威流程；后者复用 EditRoomPage 的指定 ID、topic/version 读取与原保存/关闭/重开合同。列表不调用 topic，不写旧房配置。
- “创建新房间”只在权威当前 ACTIVE 公会的 OWNER 身份下展示；提交不带旧 roomId/version，仍用 POST `/app-mini-api/mini/v1/rooms`。保留原写入防重，不自动重提；后端决定是否超额，40998 原文展示。客户端不猜房间配额（当前 App DTO 未返回配额）。
- 所有新增入口的异步完成均核对 session identity generation；A→B→A 后旧列表/创建结果不可回写或 push。换号后需重新打开页面。
- Mock 生命周期按 ID 保存多房，默认上限 1，可注入初始房间/上限；关闭房仍计数。Mock 入房按选择的 ID 查配置，第二间关闭房仍保持 owner snapshot-only 语义。
- Community `MEMBER` 和历史 `ADMIN` 均显示“主播”；历史 ADMIN 不获得审核/禁言/移出权限。仅 OWNER 可管理，Mock 默认当前用户 10001 与 ownerUserId/成员列表一致。普通主播和历史 ADMIN 的 Mock 管理写均拒绝，退会仍调用既有权威 API，不实现金额。
- 会长详情增加“名下房间”。普通公会详情仍显示现有 Backend `g.room_id` 投影；不能据此伪造全公会多房列表。
- App 原本没有创建公会、解散或转让会长接口/页面；本批不生造接口。没有新增退役页面 ID，RM-001/SC-001/SC-002 仍保留，页面数不变。

## 测试证据

SDK 实际核验：`/Users/kongzheng/fvm/versions/3.44.7/bin/flutter`，Flutter 3.44.7 / Dart 3.12.2。
未修改 SDK/fvm 源码或配置。

RED：旧代码 4 项业务断言失败（两种首页视口仍自动编辑首房、旧 ADMIN parser 仍授管理角色、角色 label/权限不符合 S17）；23 项通过。没有用编译失败充当业务 RED。

最终单次去重执行 **16 个测试文件，146 PASS，exit 0**；早先分批结果不叠加计数：

```sh
/Users/kongzheng/fvm/versions/3.44.7/bin/flutter test --no-pub \
  test/owned_room_selection_test.dart \
  test/ordinary_create_room_entry_test.dart \
  test/community_repository_test.dart \
  test/backend_community_repository_contract_test.dart \
  test/community_presentation_contract_test.dart \
  test/community_write_reliability_test.dart \
  test/community_discovery_live_fake_cleanup_test.dart \
  test/community_visual_responsiveness_test.dart \
  test/backend_room_lifecycle_repository_contract_test.dart \
  test/backend_room_lifecycle_repository_test.dart \
  test/room_lifecycle_repository_test.dart \
  test/room_lifecycle_closed_config_test.dart \
  test/room_closed_owner_view_test.dart \
  test/room_closed_owner_rules_test.dart \
  test/product_exclusions_test.dart \
  test/m23_pages_test.dart --reporter expanded
```

`flutter analyze --no-pub` 全树 No issues，exit 0。受改 Dart 文件 format 与 `git diff --check` 均通过。
保留首页返回刷新、审核状态历史、入会/禁言写可靠性、关闭房权限及产品删除负向回归；没有 skip、删除有效断言或放宽门禁。
旧“ADMIN 可以治理”的语义由主播负向用例替代；原正向治理使用 canonical OWNER，不能把旧权限当 PASS。

## 变更文件范围

- `lib/features/community/{domain/community_models.dart,data/backend_community_repository.dart,data/mock_community_repository.dart,presentation/community_pages.dart,presentation/community_hub_page.dart,presentation/guild_home_pages.dart,presentation/guild_members_pages.dart}`。
- `lib/features/room/{domain/room_lifecycle_models.dart,domain/room_lifecycle_repository.dart,data/backend_room_lifecycle_repository.dart,data/mock_room_lifecycle_repository.dart,data/mock_room_repository.dart,presentation/create_room_page.dart}`。
- 新增 `test/owned_room_selection_test.dart`；更新 `test/{ordinary_create_room_entry_test,community_repository_test,backend_community_repository_contract_test,backend_room_lifecycle_repository_contract_test,community_presentation_contract_test,product_exclusions_test,m23_pages_test}.dart`。
- `integration_test/m2_4_community_flow_test.dart` 仅同步受影响入口/按钮名称及 canonical owner fixture；本次未运行，不能计 PASS。
- 本交接文档。

## 精确剩余与未执行项

1. 按本批 community/必要 room 限定范围，未改共用 QA 模型：`lib/debug/qa_console/qa_models.dart:13-14` 仍有历史 guildMember/guildAdmin；`qa_page_catalog.dart:898` 仍将 guildAdmin 预置映射为 RoomRole.moderator。主线需统一退役该 debug 角色映射，不能拿它证明产品授权。`qa_page_catalog.dart:870/874` 与 `lib/app/page_manifest.dart:96` 的 SC-002 名称仍为历史“成员管理”。
2. 未运行或更新 golden。预期受影响：`test/goldens/{,linux/}m3_3_all/{rm-001,sc-001,sc-002}_390x844.png`；CommunityHub 无独立该组 pageID，布局已用非图像 widget 回归覆盖。
3. 未运行设备、模拟器、integration runner、构建、DB、厂商或全量测试。旧 M2.4 community runner 仍含已退役 CP 等后续流程，且 `remainingApplications isEmpty` 是旧的“不保留处理历史”断言；仅同步本批命名不能把整个 runner 算 PASS。
4. 不扩展公会全房列表的新 API，不做资金/退会转换，不碰 dynamic/榜单、钱包/支付/收益组件、staff、Backend。

## 分币方案只读裁决

已先行回复：余量币方案技术可行，不更改旧整币价值、冻结或 Apple 精确整币 reversal。新币余额只写最终 0..9；credit/carry/receipt 同事务固定锁序，BigInteger 计算与 string 展示，禁止调账绕过 carry。细化时应保留内部 carry 的 wallet_transaction 双币种 CREDIT/DEBIT（仅用户列表隐藏），carry business ID 绑定 receipt，projection bootstrap 在余额变更前完成。本批没有写资金代码，也不把该设计裁决称为资金实现验证。
