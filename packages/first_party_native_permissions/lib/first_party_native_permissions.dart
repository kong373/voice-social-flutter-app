/// Native implementation package for the app's first-party permission
/// channel. The app owns the Dart adapter so the package intentionally exposes
/// no provider-specific API.
library first_party_native_permissions;
