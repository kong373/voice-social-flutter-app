name: M3.3 video-runtime UI acceptance

on:
  push:
    branches:
      - feat/m3-3-video-runtime-ui
  pull_request:
    branches:
      - feat/m3-2a-live-auth-readonly
    paths:
      - 'lib/**'
      - 'test/**'
      - 'integration_test/m3_3_video_runtime_ui_test.dart'
      - '.github/workflows/m33-video-runtime-ui.yml'
      - 'pubspec.yaml'
      - 'pubspec.lock'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: m33-video-runtime-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_VERSION: '3.44.7'
  JAVA_VERSION: '17'
  UI_BASE_SHA: 'dac406d394bd3a4448ee953550b6a57d313e2f3c'

jobs:
  quality:
    name: Format, analyze, tests and Android build
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout candidate
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve locked dependencies
        run: flutter pub get --enforce-lockfile

      - name: Check candidate formatting
        shell: bash
        run: |
          set -Eeuo pipefail
          base="$UI_BASE_SHA"
          git cat-file -e "${base}^{commit}"
          mapfile -d '' files < <(
            git diff --name-only -z --diff-filter=ACMR "$base" HEAD -- '*.dart'
          )
          printf 'candidate_dart_files=%s\n' "${#files[@]}"
          if (( ${#files[@]} > 0 )); then
            dart format --output=none --set-exit-if-changed "${files[@]}"
          fi

      - name: Analyze
        run: flutter analyze

      - name: Run all Dart and widget tests
        run: flutter test --concurrency=1 --timeout=90s --reporter=expanded

      - name: Build Android debug APK
        run: >-
          flutter build apk --debug
          --dart-define=BACKEND_MODE=mock
          --dart-define=ENABLE_VIDEO_RUNTIME_DEMO=true

      - name: Verify no provider secrets or retired entries
        shell: bash
        run: |
          set -Eeuo pipefail
          ! grep -RInE \
            '(M3_OAUTH_CLIENT_SECRET|OAUTH_CLIENT_SECRET|RTC_APP_CERTIFICATE|IM_ADMIN_SECRET|PAYMENT_PRIVATE_KEY|BEGIN (RSA |EC )?PRIVATE KEY)' \
            lib android ios build/app/outputs/flutter-apk/app-debug.apk
          ! grep -RInE \
            '(红包|KTV|点歌|演唱|合唱|盲盒|魔法球|团子|情书|随机遇见|附近的人)' \
            lib

      - name: Upload quality evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m33-quality-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            build/app/outputs/flutter-apk/app-debug.apk
            pubspec.lock
          if-no-files-found: warn
          retention-days: 14

  avd:
    name: ${{ matrix.id }} API ${{ matrix.api }} ${{ matrix.viewport }}
    needs: quality
    if: ${{ needs.quality.result == 'success' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 75
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - id: AVD-A
            api: 36
            profile: pixel_7_pro
            viewport: 390x844
            physical: 1170x2532
            density: 480
            dpr: '3'
          - id: AVD-B
            api: 35
            profile: pixel_2
            viewport: 360x800
            physical: 900x2000
            density: 400
            dpr: '2.5'

    steps:
      - name: Checkout candidate
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve locked dependencies
        run: flutter pub get --enforce-lockfile

      - name: Require KVM
        shell: bash
        run: |
          set -Eeuo pipefail
          test -c /dev/kvm
          sudo chmod 0666 /dev/kvm

      - name: Run exact-viewport interaction acceptance
        uses: ReactiveCircus/android-emulator-runner@v2
        env:
          QA_AVD_ID: ${{ matrix.id }}
          QA_EXPECTED_VIEWPORT_WIDTH: ${{ split(matrix.viewport, 'x')[0] }}
          QA_EXPECTED_VIEWPORT_HEIGHT: ${{ split(matrix.viewport, 'x')[1] }}
          QA_EXPECTED_DPR: ${{ matrix.dpr }}
          EVIDENCE_ROOT: ${{ github.workspace }}/artifacts/m3.3/${{ matrix.id }}
        with:
          api-level: ${{ matrix.api }}
          target: google_apis
          arch: x86_64
          profile: ${{ matrix.profile }}
          cores: 2
          ram-size: 4096M
          heap-size: 512M
          disk-size: 6144M
          force-avd-creation: true
          emulator-boot-timeout: 600
          disable-linux-hw-accel: false
          disable-animations: false
          emulator-options: >-
            -no-window
            -gpu swiftshader_indirect
            -noaudio
            -no-boot-anim
            -no-snapshot
            -wipe-data
            -no-metrics
            -camera-back none
            -camera-front none
          script: |
            set -Eeuo pipefail
            mkdir -p "$EVIDENCE_ROOT/screenshots"
            adb shell wm size ${{ matrix.physical }}
            adb shell wm density ${{ matrix.density }}
            adb shell settings put system font_scale 1.0
            adb shell settings put global window_animation_scale 0
            adb shell settings put global transition_animation_scale 0
            adb shell settings put global animator_duration_scale 0
            adb logcat -c
            adb logcat -v threadtime >"$EVIDENCE_ROOT/logcat.txt" &
            logcat_pid=$!
            trap 'kill "$logcat_pid" 2>/dev/null || true' EXIT

            flutter drive \
              --driver=test_driver/integration_test.dart \
              --target=integration_test/m3_3_video_runtime_ui_test.dart \
              -d emulator-5554 \
              --dart-define=BACKEND_MODE=mock \
              --dart-define=ENABLE_VIDEO_RUNTIME_DEMO=true \
              --dart-define=QA_AVD_ID=${{ matrix.id }} \
              --dart-define=QA_EXPECTED_VIEWPORT_WIDTH=${{ split(matrix.viewport, 'x')[0] }} \
              --dart-define=QA_EXPECTED_VIEWPORT_HEIGHT=${{ split(matrix.viewport, 'x')[1] }} \
              --dart-define=QA_EXPECTED_DPR=${{ matrix.dpr }} \
              2>&1 | tee "$EVIDENCE_ROOT/flutter-drive.log"

            find . -type f -name 'm33-*.png' -print -exec cp {} "$EVIDENCE_ROOT/screenshots/" \;
            screenshot_count="$(find "$EVIDENCE_ROOT/screenshots" -type f -name '*.png' | wc -l | tr -d ' ')"
            hard_errors="$(grep -Eci 'FATAL EXCEPTION|AndroidRuntime|ANR in|MissingPluginException|RenderFlex overflow|Unhandled Exception' "$EVIDENCE_ROOT/logcat.txt" || true)"
            {
              echo "conclusion=PASS"
              echo "run_id=${{ github.run_id }}"
              echo "run_attempt=${{ github.run_attempt }}"
              echo "git_sha=${{ github.sha }}"
              echo "avd=${{ matrix.id }}"
              echo "api=${{ matrix.api }}"
              echo "viewport=${{ matrix.viewport }}"
              echo "dpr=${{ matrix.dpr }}"
              echo "screenshots=$screenshot_count"
              echo "hard_errors=$hard_errors"
              echo "provider_calls_made=false"
            } | tee "$EVIDENCE_ROOT/summary.txt"
            test "$screenshot_count" -ge 8
            test "$hard_errors" -eq 0

      - name: Upload AVD evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m33-${{ matrix.id }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts/m3.3/${{ matrix.id }}/
          if-no-files-found: warn
          retention-days: 30

  verdict:
    name: Strict two-AVD verdict
    needs: [quality, avd]
    if: always()
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Download evidence
        uses: actions/download-artifact@v4
        with:
          pattern: m33-AVD-*
          path: evidence
          merge-multiple: false

      - name: Verify both exact-viewport runs
        shell: bash
        env:
          QUALITY: ${{ needs.quality.result }}
          AVD: ${{ needs.avd.result }}
        run: |
          set -Eeuo pipefail
          test "$QUALITY" = success
          test "$AVD" = success
          mapfile -t summaries < <(find evidence -type f -name summary.txt | sort)
          test "${#summaries[@]}" -eq 2
          grep -R '^avd=AVD-A$' evidence
          grep -R '^avd=AVD-B$' evidence
          grep -R '^viewport=390x844$' evidence
          grep -R '^viewport=360x800$' evidence
          test "$(grep -R '^conclusion=PASS$' evidence | wc -l | tr -d ' ')" -eq 2
          test "$(grep -R '^hard_errors=0$' evidence | wc -l | tr -d ' ')" -eq 2
          test "$(grep -R '^provider_calls_made=false$' evidence | wc -l | tr -d ' ')" -eq 2
          {
            echo "tested_git_sha=${{ github.sha }}"
            echo "quality=$QUALITY"
            echo "avd_matrix=$AVD"
            echo "conclusion=VIDEO_RUNTIME_UI_PASS"
          } | tee aggregate-verdict.txt

      - name: Upload aggregate verdict
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m33-aggregate-${{ github.run_id }}-${{ github.run_attempt }}
          path: aggregate-verdict.txt
          if-no-files-found: error
          retention-days: 30
